#pragma once

namespace templates {
	const char* Enum =
#include "templates/enum.inc"
		;
	const char* Header =
#include "templates/header.inc"
		;
	const char* Object =
#include "templates/object.inc"
		;
}